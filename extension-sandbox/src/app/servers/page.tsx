'use client';

import { SandboxLayout, ServerManager } from '@/components';

export default function ServersPage() {
    return (
        <SandboxLayout>
            <ServerManager />
        </SandboxLayout>
    );
}
